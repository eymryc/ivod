import { BrowseCatalog } from "@/components/catalog/BrowseCatalog";
import { WEB_SERIES_SECTION } from "@/lib/catalog/sections";

export default function WebSeriesPage() {
  return <BrowseCatalog section={WEB_SERIES_SECTION} />;
}
