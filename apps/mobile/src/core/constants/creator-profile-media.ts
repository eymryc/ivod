/** Spécifications visuels profil créateur — aligné sur apps/web/lib/constants/creator-profile-media.ts */

export const CREATOR_AVATAR_RATIO_LABEL = "1:1 (carré)";
export const CREATOR_AVATAR_RECOMMENDED_PX = "800 × 800 px";

export const CREATOR_BANNER_RATIO_LABEL = "3:1 (paysage)";
export const CREATOR_BANNER_RECOMMENDED_PX = "1500 × 500 px";

export const CREATOR_PROFILE_MEDIA_HINT =
  `Uploadez depuis le Creator Studio web pour un rendu optimal :\n` +
  `· Avatar — ${CREATOR_AVATAR_RATIO_LABEL} (${CREATOR_AVATAR_RECOMMENDED_PX})\n` +
  `· Bannière — ${CREATOR_BANNER_RATIO_LABEL} (${CREATOR_BANNER_RECOMMENDED_PX})`;
